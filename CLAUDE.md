# Matchkeeper (PadelOS) — قواعد العمل مع Claude Code

هذا الملف بيوضّح إزاي نتعامل مع المشروع، وهو أول حاجة لازم تتقرا قبل أي شغل هنا.

## 1. قواعد الـ Versioning

- **رقم النسخة الأساسي في مكان واحد:** `src/App.jsx` → `const APP_VERSION = "V0.0X.YY";` (حاليًا حوالين السطر 125).
- **لازم يتزامن مع مكان تاني كمان — اسم التطبيق الأندرويد (Native):** `android/app/src/main/res/values/strings.xml` (`app_name` و `title_activity_main`). ده مش بيتحدث لوحده — لازم تعدّله يدويًا يطابق نفس رقم الـ APP_VERSION في نفس اللحظة، وإلا اسم التطبيق اللي بيظهر وقت الـ install (زي في شاشة "Do you want to install this app?") هيفضل قديم حتى لو الكود جواه محدّث. (اكتشفنا الباج ده لما ظهر "Matchkeeper V0.05.31" في شاشة التثبيت رغم إن الكود كان فعليًا V0.06.00.)
- **⚠️ لازم يتزامن مع مكان تالت: `android/app/build.gradle` → `versionCode` (رقم صحيح) و `versionName`.** ده الرقم اللي أندرويد نفسه (مش أي نص جوه التطبيق) بيستخدمه عشان يقرر إن الـ APK الجديد فعلاً "أحدث" من المتثبت. اتكشف باج حقيقي في 2026-08-23: `versionCode` فضل واقف على `13` لعشرات الـ APK builds المتتالية طول الجلسة دي (رغم إن `APP_VERSION` جوه الكود كان بيتزود كل مرة) — النتيجة إن تثبيت APK "جديد" كان مش حاسس إنه فعلاً تحديث حقيقي. **القاعدة:** كل مرة APK حقيقي يتبني (خطوة #7 تحت)، زوّد `versionCode` بواحد (`14`, `15`, ...) وحدّث `versionName` يطابق نفس رقم `APP_VERSION` (من غير الـ `V` الأول، زي `"0.10.30"`).
- **رقم الـ APK اللي بيظهر في زرار "📱 Android App" مش constant يدوي تاني — بقى ذاتي التحديث.** كان فيه `const LATEST_APK_VERSION` في `src/App.jsx` بيتعدل يدويًا، وده كان بيتنسى فعليًا (اكتشفنا إن الزرار فضل واقف على V0.10.00 لحد ما الكود وصل V0.10.08). دلوقتي التطبيق بيعمل `fetch` لملف `releases/latest.json` على الـ Hosting (نفس فكرة `dist/version.json` اللي بيتكتب أوتوماتيك كل build ويشغّل بانر "نسخة جديدة" على الويب)، وده بيبقى الـ source of truth الحقيقي. `LATEST_APK_VERSION_FALLBACK` لسه موجود في الكود بس **fallback بس** لحد ما الـ fetch يخلص أو لو فشل (offline). **القاعدة الجديدة:** أي مرة APK حقيقي يتبني ويتحط في `dist/releases/` (خطوة #7 تحت)، **لازم** ملف `dist/releases/latest.json` بمحتوى `{"version":"V0.0X.YY"}` يتكتب أو يتحدّث في نفس اللحظة قبل الـ `firebase deploy --only hosting`، وإلا الزرار هيفضل واقف على آخر رقم منشور قبل كده.
- **متزودش رقم نسخة جديد من نفسك.** الافتراضي إنك تسيب الرقم زي ما هو وانت شغال/بتعدل.
- **الاستثناء الوحيد:** أول ما تكون جاهز تسلّم **ملف حقيقي جاهز للتجربة أو البناء** (يعني بنيت الـ web build فعليًا و/أو عملت `cap sync` وجهزت الأندرويد للتجربة على الموبايل) — في اللحظة دي، وبس فيها، زوّد رقم النسخة أوتوماتيك من غير ما تسأل.
- الترقيم بيتبع نفس نمط الـ CHANGELOG الموجود: `V0.0X.YY` (زيادة YY لتعديل عادي، وزيادة X لو حسيت إنه جديد بما يكفي — اتبع نفس الحس اللي شايفه في تاريخ الـ CHANGELOG).

## 2. قاعدة الـ CHANGELOG.md

- أي مرة بتزوّد رقم نسخة (يعني أي مرة بتسلّم ملف جاهز للتجربة)، **لازم تضيف entry جديد فوق في `CHANGELOG.md`** بنفس الأسلوب الموجود (عربي، رقم النسخة كعنوان `##`، نقط bullet توضح اللي اتغير وليه، خط فاصل `---` بعدها).
- لو التعديل بسيط وبيتضاف لنفس رقم نسخة موجود بالفعل من غير ما ترفع الرقم (زي بعض الأمثلة في التاريخ: "تحديث على نفس الإصدار")، وضّح ده صراحة في الـ entry نفسه.
- ماتكتبش في CHANGELOG إلا وقت تسليم ملف حقيقي — مش أثناء الشغل النظري أو المسودات.
- **⚠️ من V0.11.32: أي إنترى بتضيفه في `CHANGELOG.md` (عربي)، لازم تضيف إنترى مقابله بالإنجليزي في `CHANGELOG_EN.md`.** ده الملف اللي شاشة "📋 Version Updates" جوه التطبيق (زرار في القايمة العلوية ⚙️، يظهر للأدمن Platform Admin بس) بتقرا منه مباشرة (`import ... from "../CHANGELOG_EN.md?raw"` في `src/App.jsx`) — ده مقصود يكون بالإنجليزي لإن التطبيق نفسه إنجليزي بالكامل، والـ Arabic الأصلي في `CHANGELOG.md` هيفضل مش مقروء بسهولة جوه الشاشة دي. نفس الصيغة بالظبط (`## VX.X.X — عنوان` وبعده bullets وبعدها `---`) — `CHANGELOG_EN.md` **مش لازم يغطي كل تاريخ المشروع** (بدأ فاضي من V0.11.32 لحد دلوقتي، مفيهوش الإصدارات القديمة قبل كده)، بس أي إصدار جديد من هنا لازم يتسجل في الاتنين مع بعض.

## 3. قاعدة الـ BUGS.md

- لما الأدمن يقول "سجّل الباج ده" أو يطلب رقم لباج، **ضيفه في `BUGS.md`** (جذر المشروع، قسم "📜 السجل الكامل") برقم ثابت جديد (آخر رقم +1، الأرقام متتاخدش من باج اتشال)، وحالته 🔴 مفتوح، وتاريخ التسجيل.
- ده تسجيل بس — **متصلحش الباج نفسه إلا لو اتطلب صراحة.**
- لما باج من القايمة يتصلح فعلاً (ويتسلم في نسخة)، غيّر حالته لـ ✅ اتصلح واذكر رقم النسخة اللي اتصلح فيها، من غير ما تشيله من الملف — **وشيله من قسم "🗺️ الخريطة" لو كان فيها** (الخريطة للباجات المفتوحة بس).
- **الملف فيه كمان قسم "🗺️ الخريطة (The Map)"** — ترتيب مقترح لمعالجة الباجات **المفتوحة بس** حسب حجم الخطر (Blast radius) والتعرّض وسهولة الإصلاح. **أي باج جديد يتسجل، حطه في قسم "⚪ برّه الخريطة لسه" فقط — متحطوش في الخريطة نفسها إلا لو الأدمن طلب صراحة نحدد مكانه.**

## 4. قاعدة الـ ENHANCEMENTS.md

- لما الأدمن يقول "سجّل التحسين ده" أو يطلب رقم لتحسين/ميزة، **ضيفه في `ENHANCEMENTS.md`** (جذر المشروع، قسم "📜 السجل الكامل") بنفس أسلوب `BUGS.md` بالظبط: رقم ثابت جديد (آخر رقم +1)، حالته 🔴 لسه معمول، وتاريخ التسجيل.
- ده تسجيل بس — **متنفّذش التحسين نفسه إلا لو اتطلب صراحة.**
- لما تحسين من القايمة يتنفذ فعلاً (ويتسلم في نسخة)، غيّر حالته لـ ✅ اتعمل واذكر رقم النسخة، من غير ما تشيله من الملف.
- **الملف فيه كمان قسم "🗺️ الخريطة (The Map)"** — ترتيب مقترح للتنفيذ (إيه الأول وإيه الأخير) حسب سهولة البناء + الأهمية + القيمة للمستخدم. **أي تحسين جديد يتسجل، حطه في قسم "⚪ برّه الخريطة لسه" فقط — متحطوش في الخريطة نفسها إلا لو الأدمن طلب صراحة نحدد مكانه.**

## 5. مسارات مهمة على اللابتوب

- **جذر المشروع:** `C:\Users\Yoga 260\PadelOS2`
- **كل الأبلكيشن لوجيك (الويب/الشاشات):** `src/App.jsx` (ملف واحد كبير ~6700 سطر)
- **الـ entry point:** `src/main.jsx`
- **Cloud Functions (Firebase):** `functions/index.js`
- **الأندرويد (native shell):** `android/`
  - كود الـ Match Mode Native (Kotlin/Java plugin, foreground service, alarm receivers): `android/app/src/main/java/com/trimachine/padelos/`
  - Build output web: `dist/` (الويب build اللي بيتنقل للأندرويد بعد `cap sync`)
  - **⚠️ الأندرويد بقى فيه product flavors (prod/dev) بعد إضافة بيئة DEV** — `gradlew assembleDebug` لوحده دلوقتي بيبني الـ 2 flavor مع بعض. الأمر الصح للتسليم العادي (إنتاج) هو **`gradlew assembleProdDebug`** (ده اللي يعادل الأمر القديم `assembleDebug` قبل الـ flavors)، والـ APK بيطلع في `android/app/build/outputs/apk/prod/debug/app-prod-debug.apk`. لو محتاج نسخة DEV (بتتصل بـ `padelos-dev` مش الإنتاج الحقيقي، package مختلف `com.trimachine.padelos.dev` عشان تتثبت جنب النسخة الحقيقية على نفس الموبايل) استخدم `gradlew assembleDevDebug` → `android/app/build/outputs/apk/dev/debug/app-dev-debug.apk`.
  - Android SDK: `C:\Users\Yoga 260\AppData\Local\Android\Sdk` (متسجل في `android/local.properties`)
- **سكريبتات جاهزة في الجذر (bat files):**
  - `_01 npm run build.bat` → بناء الويب
  - `_02 npx cap sync.bat` → نقل الـ build للأندرويد
  - `_03 npx cap open android.bat` → فتح Android Studio
  - `_04 Record Android Log.bat` → تسجيل logcat في `Downloads\test_log.txt`
- **GitHub remote:** `https://github.com/ElMiko-max/PadelOS2.git` (origin, main branch)
- **Firebase project (Production):** `padelos-6f999` — Hosting بتاعه مستخدم بس لتوزيع ملفات الـ APK (`dist/releases/`)، مش لتشغيل ويب أب حقيقي للمستخدمين (ده بيحصل على Vercel، شوف تحت). رابط الـ Hosting: `https://padelos-6f999.web.app`
- **Firebase project (DEV):** `padelos-dev` — **ليه Hosting شغال فعليًا بويب أب حقيقي قابل للفتح من المتصفح**، مختلف تمامًا عن الاستخدام في الإنتاج فوق. الرابط: `https://padelos-dev.web.app`. البناء بيتعمل بـ `.env.development` (`VITE_FIREBASE_PROJECT_ID=padelos-dev`, `VITE_ENV_LABEL=development`) عن طريق `npm run build:dev` (مش `npm run build` العادي)، وده اللي بيخلي التطبيق يوصل فعليًا بـ Firestore بتاع `padelos-dev` بدل الإنتاج، وتظهر "· DEV" جنب رقم النسخة في الـ TopBar. **⚠️ لازم تحذر:** `dist/` واحد بس، يعني بناء dev بيمسح ويستبدل أي `dist/` كان فيه بناء إنتاج (بما فيه `dist/releases/` و `dist/version.json`) — لو عملت `npm run build:dev` لازم تعمل `git checkout -- dist/ && git clean -fd dist/` بعد الـ deploy عشان ترجّع `dist/` لحالة الإنتاج المتزامنة مع الـ git commit، وإلا هتفضل `dist/` local مش متطابقة مع اللي متعمل له commit، وأي حد يعمل build/deploy تاني من غير ما ينتبه ممكن يسلّم نسخة dev غلط أو يمسح release APK بيتوقع لسه موجود. الأدمن استخدم الرابط ده تاريخيًا كـ "ويب أب dev" — لو قال "ship to dev" أو "حدّث الويب دف"، قصده الـ deploy ده تحديدًا: `npm run build:dev` ثم `firebase deploy --only hosting --project dev` ثم استرجاع `dist/` زي فوق.
- **جوه التطبيق نفسه (Platform Admin → Data & Backup → Other Tools):** زرار "🧪 Open DEV Environment" (بيفتح `padelos-dev.web.app` مباشرة) وزرار "☁️ Clone Data to DEV" (بينسخ بيانات الإنتاج المحمّلة حاليًا إلى `padelos-dev` — أداة منفصلة تمامًا عن الـ deploy، دي نسخ بيانات مش نسخ كود).

**⚠️ في مفهوم الأدمن، تسليم عادي فيه مخرجين (outputs): (1) الـ APK، و(2) GitHub.** لما يقول "الويب أب" (web app) من غير ما يحدد dev، قصده **GitHub** (الكود) — مش رابط الـ `padelos-6f999.web.app` نفسه. إن `dist/index.html` بيتنقل لنفس الـ Hosting كنتيجة جانبية لـ `firebase deploy --only hosting` (لإنه بينشر مجلد `dist/` كله مش بس `dist/releases/`) ده تفصيلة تقنية مش مخرج حقيقي — متتكلمش عنه كـ "الويب أب اتحدث" أبدًا. **الاستثناء:** لو قال صراحة "ship to dev" أو "حدّث/دف الويب دف"، فده مخرج تالت حقيقي ومختلف — `padelos-dev.web.app` (شوف قاعدة #5 فوق) — ومختلف تمامًا عن أي حاجة تانية اسمها "DEV" في السياق ده (زي APK بتاع `assembleDevDebug`، اللي هو تسليم رابع منفصل تمامًا، مش نفس حاجة الويب دف).

## 6. قاعدة تبليغ المسار

**أي مرة تسلّمني ملف** (build، APK، صورة، أي output)، **قولي المسار الكامل بالظبط** في الرسالة — مش وصف عام، المسار الفعلي على الجهاز (زي `C:\Users\Yoga 260\PadelOS2\dist\...` أو مكان الـ APK بعد الـ build).

## 7. الـ Workflow المتوقع

الدور بتاعي (Claude) في كل مرة أسلّم فيها شغل جاهز للتجربة:
1. أعمل التعديلات في الكود.
2. أزوّد رقم الـ APP_VERSION (قاعدة #1) وأكتب الـ CHANGELOG entry (قاعدة #2).
3. أعمل build للويب (`npm run build`) و `cap sync` للأندرويد تلقائي من غير ما أستنى تعليمات.
4. أعمل commit لـ GitHub تلقائي (نفس remote أعلاه) — من غير ما أستنى إذن كل مرة.
5. **استثناءات لازم تأكيد صريح من الأدمن كل مرة قبل ما تحصل:**
   - **بناء الـ APK نفسه** (`gradlew assembleProdDebug` — قاعدة #5 التسليم العادي — أو أي build أندرويد فعلي تاني، بما فيها `assembleDevDebug`).
   - **الـ push** لـ GitHub (`git push`).
   الباقي (كود، version، CHANGELOG، web build، cap sync، commit محلي) بيحصل أوتوماتيك من غير ما أستنى.
6. أقول المسار بالظبط لأي ملف تجربة جاهز (الويب build أو الـ APK) — قاعدة #6.
7. **تسليم الـ APK فعليًا:**
   - **أول حاجة:** أعمل rename للـ APK عشان يعكس اسم البرنامج ورقم النسخة، بالصيغة: `Matchkeeper-V0.0X.YY-debug.apk`.
   - **⚠️ ممنوع تمامًا حط أي ملف APK جوه `public/`.** `public/` هو مصدر الويب app نفسه — أي حاجة فيه بتتنقل لـ `dist/` وقت `npm run build`، **وبعدين `cap sync` بينقل `dist/` بالكامل جوه الأندرويد نفسه** (`android/app/src/main/assets/public/`). لو الـ APK كان قاعد في `public/` وقت الـ build الجاي، هيتحط جوه الـ APK الجديد نفسه — وده باج حقيقي حصل بالفعل في V0.06.02 (كل نسخة كانت بتحتوي كل النسخ اللي قبلها، وصل الحجم لـ 56 ميجا من 11 ميجا الأصلية، وGitHub حذّر إنه أكبر من الحد المسموح).
   - **⚠️ `dist/releases/` نفسه بردو أبدًا ميتحطش فيه حاجة يدوي مباشرة — فيه pipeline دايم موجود لازم يتستخدم:** `release-archive/` (في جذر المشروع، برّه `dist/` خالص، متلمسوش الـ Vite build ولا `cap sync`) هو المخزن **الدائم** اللي فيه دايمًا APK واحد بس (آخر نسخة اتشحنت فعليًا) — ده الـ source of truth الحقيقي، مش `dist/releases/` نفسه (اللي بيتمسح ويتبني من جديد كل مرة، مؤقت تمامًا). `scripts/prepare-hosting-release.js` هو الوحيد المسموح يكتب جوه `dist/releases/` — بيقرا كل ملفات `.apk` من `release-archive/` وينسخهم هناك.
   - **الترتيب الصحيح دايمًا:** (1) `npm run build` (2) `npx cap sync` (3) `gradlew assembleProdDebug` (مش `assembleDebug` لوحدها — بعد الـ product flavors بقى فيه prod/dev، والأمر ده هو اللي بيبني نسخة الإنتاج بس؛ الـ APK بيطلع في `android/app/build/outputs/apk/prod/debug/app-prod-debug.apk`) — في اللحظة دي `dist/` و`android/assets` نضيفين تمامًا من أي APK — **(4)** امسح أي APK قديم من `release-archive/` وحط الـ APK الجديد فيه بس بالاسم `Matchkeeper-V0.0X.YY-debug.apk` (عشان يفضل فيه نسخة واحدة بس دايمًا، آخر واحدة) **(4.5)** شغّل `node scripts/prepare-hosting-release.js` عشان ينسخ من `release-archive/` لـ `dist/releases/` **(4.75)** اكتب/حدّث `dist/releases/latest.json` بمحتوى `{"version":"V0.0X.YY"}` بنفس رقم النسخة بالظبط — ده اللي بيخلي زرار "Android App" في التطبيق يعرض الرقم الصح أوتوماتيك من غير تعديل يدوي تاني في `App.jsx` **(5)** `firebase deploy --only hosting` فورًا من غير ما تعمل `npm run build` تاني بعد كل ده (لإن ده هيمسح الـ APK والـ latest.json اللي حطيتهم في `dist/`).
   - **ملحوظة CORS:** زرار "Android App" بيعمل `fetch` لملف `releases/latest.json` من عنوان `padelos-6f999.web.app` — لكن الويب app الحقيقي بيشتغل من دومين تاني (`matchkeeper.app`, Vercel)، يعني ده طلب **cross-origin**. عشان كده `firebase.json` فيه header `Access-Control-Allow-Origin: *` مضاف على `/releases/**` تحديدًا — من غير الـ header ده الـ fetch بيتمنع بصمت (بيرجع للـ fallback القديم جوه الكود من غير أي error ظاهر) وده باج حقيقي حصل بالظبط في V0.10.09 قبل ما يتصلح. لو حد شال الـ header ده بالغلط مستقبلًا، نفس الباج هيرجع.
   - أديه لينك مباشر بالصيغة `https://padelos-6f999.web.app/releases/Matchkeeper-V0.0X.YY-debug.apk` — الأدمن بيفتحه من متصفح الموبايل في أي وقت، من غير ما يحتاج اللابتوب يكون شغال أو حتى مفتوح. **ملحوظة أمان:** اللينك ده عام (public) — أي حد معاه اللينك يقدر يحمّله، مفيش باسورد عليه.
   - **الطريقة الاحتياطية:** أبعته كمان جوه محادثة Claude (عن طريق أداة إرسال الملفات) كخيار إضافي — لكن جُرّب وطلع مش موثوق دايمًا (بيفضل "downloading..." من غير ما يخلص) خصوصًا للملفات الكبيرة زي الـ APK، فمتعتمدش عليه لوحده.
   - **مش متاح حاليًا:** إرسال بالإيميل أو تيليجرام — مفيش أداة عندي لأي منهم. لو الأدمن عايز ده مستقبلًا، محتاج setup منفصل (زي Telegram Bot API token) قبل ما يبقى ممكن.

دور المستخدم بس: يجرب الـ web app أو يعمل install لل APK على الموبايل ويجرب، وياخد قرار الـ APK build والـ push كل مرة — مش المفروض يشغّل بنفسه أوامر build/sync يدويًا.

> ملاحظة أمان: أي عملية هدّامة (force push، حذف فروع، reset --hard) برّه النطاق ده وتحتاج تأكيد صريح دايمًا.
